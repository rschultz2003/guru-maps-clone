# Atlas Maps (Phase 1)

```bash
cd apps/mobile
npm install
npx expo start
```

Long-press the map to drop a pin. Upload a photo as the pin icon. Folders persist in SQLite.
MapLibre demo style is online until offline packs land in Phase 3.
